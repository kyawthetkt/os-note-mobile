import React from 'react';
import {ScrollView, View, Text} from 'react-native';

export default () => {
  return (
    <ScrollView>
      <View>
        <Text>This is home!</Text>
      </View>
    </ScrollView>
  );
};
