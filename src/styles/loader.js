import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    marginTop: 25,
  },
  textWrapper: {
    color: 'red',
    textAlign: 'center',
    fontSize: 18,
  },
});
